name: Trigger auto deployment for alertas

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/alertas-AutoDeployTrigger-d7e940cc-90d5-4507-a8b3-113f9d21fbaf.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ALERTAS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.ALERTAS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.ALERTAS_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: containerregistrydemofacunav.azurecr.io
          registryUsername: ${{ secrets.ALERTAS_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.ALERTAS_REGISTRY_PASSWORD }}
          containerAppName: alertas
          resourceGroup: topic-resource-group
          imageToBuild: containerregistrydemofacunav.azurecr.io/alertas:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


