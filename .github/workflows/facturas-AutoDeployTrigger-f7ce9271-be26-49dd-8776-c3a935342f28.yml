name: Trigger auto deployment for facturas

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/facturas-AutoDeployTrigger-f7ce9271-be26-49dd-8776-c3a935342f28.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.FACTURAS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.FACTURAS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.FACTURAS_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: containerregistrydemofacunav.azurecr.io
          registryUsername: ${{ secrets.FACTURAS_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.FACTURAS_REGISTRY_PASSWORD }}
          containerAppName: facturas
          resourceGroup: DefaultResourceGroup-CQ
          imageToBuild: containerregistrydemofacunav.azurecr.io/facturas:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


